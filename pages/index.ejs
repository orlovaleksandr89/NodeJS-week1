<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- CSS only -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <title><%= title %></title>
  </head>
  <body>
    <% if (created) {%>
    <div class="alert alert-success">Note Created successfully</div>
    <% } %>
    <div class="container mx-auto align-items-center justify-items-center">
      <h1 class="text-dark text-center">Hello from server</h1>
      <form action="/" method="post">
        <div class="form-group">
          <input
            type="text"
            id="text"
            name="title"
            placeholder="Enter your text"
            class="form-control mt-2"
            required
          />
          <button class="btn btn-primary mt-2" type="submit">Create</button>
        </div>
      </form>

      <% if (notes.length > 0) {%>

      <div class="card mt-3">
        <ul class="list-group">
          <% for(let note of notes) { %>

          <li
            class="list-group-item d-flex align-items-center justify-content-between"
          >
            <div id="text" class="m-0 p-0"><%= note.title %></div>
            <div class="d-flex justify-content-center">
              <button
                class="btn btn-warning mx-3"
                data-type="update"
                data-id="<%= note._id
              %>"
              >
                Update
              </button>
              <button
                class="btn btn-danger"
                data-type="remove"
                data-id="<%= note._id
          %>"
              >
                &times;
              </button>
            </div>
          </li>
          <% } %>
        </ul>
      </div>
      <% } else { %>
      <div class="mt-3 fw-bold">
        <h3>There are no notes. Please create one</h3>
      </div>
      <% } %>
    </div>
    <script src="./app.js"></script>
  </body>
</html>
